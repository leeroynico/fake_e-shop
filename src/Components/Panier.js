import { useState } from "react";
import { useEffect } from "react";
import React from "react";

function Panier() {
  // const [articlePanier, setarticlePanier] = useState([]);

  // useEffect(() => {
  //    setarticlePanier({props.panier});
  // }, [input])

  return <></>;
}

export default Panier;
